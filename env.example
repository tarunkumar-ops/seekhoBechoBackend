# Copy this file to `.env` (do not commit `.env`) and fill values.

# Django
DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=1
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost

# Database (defaults to sqlite if unset)
# DB_ENGINE=django.db.backends.postgresql
# DB_NAME=ecomm
# DB_USER=postgres
# DB_PASSWORD=postgres
# DB_HOST=127.0.0.1
# DB_PORT=5432

# JWT
JWT_ACCESS_MINUTES=30
JWT_REFRESH_DAYS=7

# OTP
OTP_SECRET=change-me-too

# Twilio WhatsApp (set TWILIO_ENABLED=1 to send real messages)
TWILIO_ENABLED=0
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
# Example: +14155238886
TWILIO_WHATSAPP_FROM=

